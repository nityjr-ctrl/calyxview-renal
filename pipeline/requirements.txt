# renalplan: CPU pipeline (Python 3.11+). GPU model backends (TotalSegmentator,
# nnU-Net) are installed separately in the GPU environment; see README.
numpy>=1.26
scipy>=1.11
scikit-image>=0.22
trimesh>=4.4
fast-simplification>=0.1.9
rtree>=1.1
shapely>=2.0
nibabel>=5.2
pydicom>=2.4
matplotlib>=3.8
pytest>=8
